type Event @model @auth(rules: [{allow: public}]) {
  id: ID!
  type: String!
  date: AWSDate!
  time: AWSTime!
  eventJSON: AWSJSON!
}

type User @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  email: AWSEmail!
  stravaToken: String
  dateLastUpdated: AWSDateTime
}
 